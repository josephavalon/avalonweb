/**
 * Protocol module public API.
 *
 * Import from '@/lib/protocol' — not from individual files — so we can
 * reorganize internals without touching consumers.
 */
export * from './types';
export * from './verticals';
