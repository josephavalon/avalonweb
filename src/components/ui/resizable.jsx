// Orphan shadcn shell — not used anywhere in the app.
// Re-install `react-resizable-panels` and restore from shadcn CLI if needed.
export const ResizablePanelGroup = () => null;
export const ResizablePanel = () => null;
export const ResizableHandle = () => null;
