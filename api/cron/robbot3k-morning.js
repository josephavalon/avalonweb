// Stable route used by vercel.json. Implementation lives in the descriptively
// named refresh handler so manual tests and the scheduled path share one gate.
export { default } from './robbot3k-refresh.js';
